import{Bath,Bed,Heart,MapPin,Share2}from'lucide-react';import{Link}from'react-router-dom';import Placeholder from'../common/Placeholder';import{money}from'../../utils/format';import{useFavorites}from'../../hooks/useFavorites';export default function PropertyCard({property:p,onSelect}){const{toggle,isFavorite}=useFavorites();return <article className="card" onMouseEnter={()=>onSelect?.(p.id)}><Placeholder label="Property photo placeholder"/><div className="cardBody"><b>{money(p.price,p.currency)}</b><span className="pill">{p.status} · {p.transactionType}</span><h3>{p.title}</h3><p><MapPin size={16}/> {p.city}, {p.state}</p><p><Bed size={16}/> {p.bedrooms} <Bath size={16}/> {p.bathrooms} · {p.builtArea} {p.areaUnit}</p><div className="actions"><Link className="btn small" to={`/properties/${p.slug}`}>Details</Link><button className="iconBtn" onClick={()=>toggle(p.id)}><Heart fill={isFavorite(p.id)?'currentColor':'none'}/></button><button className="iconBtn" onClick={()=>navigator.share?.({title:p.title,url:location.origin+'/properties/'+p.slug})}><Share2/></button></div></div></article>}
