import{collection,getDocs,addDoc,updateDoc,deleteDoc,doc,serverTimestamp}from'firebase/firestore';import{db,firebaseEnabled}from'../firebase/firebase';import{demoProperties}from'../data/demoData';
const c='properties';export async function getProperties(){if(!firebaseEnabled)return demoProperties;const s=await getDocs(collection(db,c));return s.docs.map(d=>({id:d.id,...d.data()}))}export async function saveProperty(data,id){if(!firebaseEnabled)return{id:id||crypto.randomUUID(),...data};return id?updateDoc(doc(db,c,id),{...data,updatedAt:serverTimestamp()}):addDoc(collection(db,c),{...data,createdAt:serverTimestamp(),updatedAt:serverTimestamp()})}export async function deleteProperty(id){if(!firebaseEnabled)return true;return deleteDoc(doc(db,c,id))}
