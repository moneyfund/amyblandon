import{useState}from'react';import{loginEmail,loginGoogle}from'../../services/authService';import{firebaseEnabled}from'../../firebase/firebase';export default function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');async function submit(e){e.preventDefault();try{await loginEmail(email,password)}catch(err){setError(err.message)}}return <section><h1>Admin login</h1>{!firebaseEnabled&&<p className="notice">Firebase is not configured. Admin access remains protected and unavailable in demo mode.</p>}<form className="form" onSubmit={submit}><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password"/><button className="btn primary">Login</button><button type="button" className="btn secondary" onClick={()=>loginGoogle().catch(e=>setError(e.message))}>Login with Google</button>{error&&<p className="error">{error}</p>}</form></section>}
