import{siteConfig}from'../config/site';const clean=n=>(n||siteConfig.whatsapp).replace(/\D/g,'');export const whatsappLink=(message,phone)=>`https://wa.me/${clean(phone)}?text=${encodeURIComponent(message)}`;export const propertyWhatsApp=p=>whatsappLink(`Hola Amy, estoy interesado/a en la propiedad “${p.title}” con precio “${p.price}”. Me gustaría recibir más información.`);export const insuranceWhatsApp=type=>whatsappLink(`Hola Amy, me gustaría recibir orientación sobre un seguro de ${type}.`);
