import{useState}from'react';import{loginEmail,loginGoogle}from'../../services/authService';import{firebaseEnabled}from'../../firebase/firebase';export default function Login(){const[email,setEmail]=useState(''),[password,setContraseña]=useState(''),[error,setError]=useState('');async function submit(e){e.preventDefault();try{await loginEmail(email,password)}catch(err){setError(err.message)}}return <section><h1>Acceso administrativo</h1>{!firebaseEnabled&&<p className="notice">Firebase no está configurado. El acceso administrativo permanece protegido.</p>}<form className="form" onSubmit={submit}><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input type="password" value={password} onChange={e=>setContraseña(e.target.value)} placeholder="Contraseña"/><button className="btn primary">Ingresar</button><button type="button" className="btn secondary" onClick={()=>loginGoogle().catch(e=>setError(e.message))}>Ingresar con Google</button>{error&&<p className="error">{error}</p>}</form></section>}
